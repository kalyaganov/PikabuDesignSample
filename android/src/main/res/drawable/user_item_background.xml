<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"
          android:exitFadeDuration="@android:integer/config_mediumAnimTime">
    <!-- Even though these two point to the same resource, have two states so the drawable will invalidate itself when coming out of pressed state. -->
    <item android:state_pressed="true" android:drawable="@drawable/user_item_selected_bg" />
    <item android:state_pressed="false" android:drawable="@drawable/user_item_bg" />
    <item android:state_focused="true" android:drawable="@drawable/user_item_focus_bg" />
</selector>